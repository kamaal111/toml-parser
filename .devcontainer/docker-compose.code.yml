services:
  code:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    container_name: toml-parser-code
    volumes:
      - ../..:/workspaces:cached
    command: sleep infinity
